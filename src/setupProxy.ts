/*
const { createProxyMiddleware } = require('http-proxy-middleware');

module.exports = function (app: any) {
  app.use(
    createProxyMiddleware('/votes', {
      target: 'https://takgyun.shop',
      changeOrigin: true,
    }),
  );
};
*/
